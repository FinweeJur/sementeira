node_modules/
dist/
dist-ts/
# `build/` e a pasta de RECURSOS do electron-builder, nao de saida — o
# icone do app mora aqui. Ignora o conteudo, menos os recursos versionados.
build/*
!build/icon.png
release/
# Chaves de API — do servidor da versão web (servidor/.env) e do dev local
# (.env.local, citado no ONBOARDING). `.env.example` é versionado de propósito.
.env
.env.*
!.env.example
*.log
.DS_Store
.zcode/
desktop.ini
